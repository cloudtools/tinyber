# -*- Mode: Python -*-

from distutils.core import setup

setup (
    name             = 'tinyber_gen',
    description      = 'code generator for tinyber',
    scripts          = ['tinyber_gen.py'],
    )
